import { ContentDefault } from '../../../shared/components/content/contentDefault';
import { NpcContent } from '../components/guides/guides';

export const NpcContentScreen = () => {
  return <ContentDefault children={<NpcContent />} />;
};

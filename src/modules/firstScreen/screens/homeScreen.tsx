import { ContentDefault } from '../../../shared/components/content/contentDefault';
import { SectionHome } from '../components/section/section';

const FirstScreen = () => {
  return <ContentDefault children={<SectionHome />} />;
};

export default FirstScreen;
